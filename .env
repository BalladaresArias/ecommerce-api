PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ecommerce_db
JWT_SECRET=mi_clave_secreta_super_segura_2024
JWT_EXPIRES_IN=24h
```

> 💡 `DB_PASSWORD` se queda vacío porque XAMPP/MariaDB no tiene contraseña por defecto.

---

### 3.2 — Crea el archivo `.gitignore`

En la raíz crea `.gitignore`:
```
node_modules/
.env